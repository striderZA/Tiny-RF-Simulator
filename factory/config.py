"""Legacy configuration is preserved during upgrade but never imported.

Configure the native consumer in .factory/consumer.json using factory init.
Provider authentication and model tiers belong to native Archon configuration.
The installed pack.json is the single entry-point and capability manifest.
"""
